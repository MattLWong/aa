<h2>New Tweet!</h2>
<form class="tweet-compose" action="/tweets" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <br>

  <label>
    <a href="#" class='add-mentioned-user'>Add Mention</a>
    <div class="mentioned-users">

    </div>
  </label>
  <br>

  <input type="Submit" value="Post Tweet!">
  <strong class="char-left"></strong>
</form>

<script class="select-mentions" type="text/template">
  <div>
    <select name="tweet[mentioned_user_ids][]">
      <option disabled selected>(Mentioned User)</option>
      <% User.all.each do |user| %>
        <option value="<%= user.id %>"><%= user.username %></option>
      <% end %>
    </select>
    <a class="remove-mentioned-user" href="#">Remove User</a>
  </div>
  </script>
