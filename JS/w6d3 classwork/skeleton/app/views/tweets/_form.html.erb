<h2>New Tweet!</h2>
<form class="tweet-compose" >
  <textarea name="tweet[content]"></textarea><br>
  <strong class="char-left"></strong><br>

  <div class="mentioned-users">
    <script type="text/template">
      <div>
        <select name="tweet[mentioned_user_ids][]">
          <% User.all.each do |user| %>
            <option value="<%= user.id %>"><%= user.username %></option>
          <% end %>
        </select>

        <a class="remove-mentioned-user" href="javascript:void(0)">Remove</a>
      </div>
    </script>

    <ul></ul>
  </div>
  <a class="add-mentioned-user" href="#">Add mention</a><br>

  <input type="submit" value="Post Tweet!">
</form>


<!-- <h2>New Tweet!</h2>
<form action="/tweets" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <br>

  <label>
    Mention
    <select name="tweet[mentioned_user_ids][]">
      <option></option>
      <% User.all.each do |user| %>
        <option value="<%= user.id %>"><%= user.username %></option>
      <% end %>
    </select>
  </label>
  <br>

  <input type="Submit" value="Post Tweet!">
</form> -->
