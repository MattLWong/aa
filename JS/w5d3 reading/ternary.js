dim = 'width'

var propertyName = ( dim === 'width' ) ? 'clientWidth' : 'clientHeight';

console.log(propertyName);

var propertySack = (dim === "height") ? 'clientWidth' : 'clientHeight';

console.log(propertySack);
